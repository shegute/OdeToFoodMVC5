@{
    //this is where the app starts and it will use the below layout like a master page for all other cshtml pages
    // or as a default layout, unless they set their own layout page separately.
    Layout = "~/Views/Shared/_Layout.cshtml";
}
